#include <bits/stdc++.h>
using namespace std;

int main() {
   int t;cin>>t;
   while(t--){
   		int n,k;cin>>n>>k;
		int a[n+1];
		int ok=0;
		for(int i=1;i<=n;i++ ){
			cin>>a[i];
			if(a[i]==k){
				ok=i;
			}
		}
		if(ok) cout<<ok;
		else cout<<"NO";	
	    cout<<endl;
   }
}
